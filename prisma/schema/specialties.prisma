model Specialties {
  id                String              @id @default(uuid())
  title             String
  icon              String?
  doctorSpecialites DoctorSpecialties[]

  @@map("specialties")
}
